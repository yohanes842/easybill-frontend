import { DialogDisplayState } from '../interfaces/dialogDisplayState';

export const initialState: DialogDisplayState = {
  usernameDialogDisplay: false,
  passwordDialogDisplay: false,
  accountNumberDialogDisplay: false,
};
